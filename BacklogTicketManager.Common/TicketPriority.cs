namespace BacklogTicketManager.Common;

/// <summary>
/// Relative urgency of a backlog ticket.
/// </summary>
public enum TicketPriority
{
    Low = 0,
    Medium = 1,
    High = 2,
    Critical = 3
}
